using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class VolvoxSize : MonoBehaviour
{
    [SerializeField] float inceaseSizeAmount;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    void Update()
    {
        //transform.localScale
    }
}
